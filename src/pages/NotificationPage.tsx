import { Flex } from "@chakra-ui/core"
import React from "react"

export interface NotificationPageProps {}

export default function NotificationPage(props: NotificationPageProps) {
  return <Flex>NOTIFICATION PAGE</Flex>
}

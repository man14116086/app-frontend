import { Flex } from "@chakra-ui/core"
import React from "react"

export interface ProfilePageProps {}

export default function ProfilePage(props: ProfilePageProps) {
  return <Flex>PROFILE PAGE</Flex>
}

import { Flex } from "@chakra-ui/core"
import React from "react"

export interface EditorPageProps {}

export default function EditorPage(props: EditorPageProps) {
  return <Flex>EDITOR PAGE</Flex>
}
